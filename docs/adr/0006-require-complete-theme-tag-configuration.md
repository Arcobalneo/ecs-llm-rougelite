# Require Complete Theme Tag Configuration

Theme tags are extensibility points for wishes, visuals, rival themes, enemy drift, fallback interpretation, and arena tinting, so adding one cannot be treated as adding a string. New theme tags must provide the supporting configuration needed to render, interpret, contrast, localize, test, and safely fallback before they are available to the wish interpreter.

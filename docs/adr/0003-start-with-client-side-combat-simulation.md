# Start With Client-Side Combat Simulation

The initial game keeps full combat simulation in the browser while the server handles wish interpretation, persistence, and basic result recording. This avoids the cost of building an authoritative simulation before the core wishcraft experience is proven, while still recording run summaries so scoring, balance, and suspicious results can be inspected later.

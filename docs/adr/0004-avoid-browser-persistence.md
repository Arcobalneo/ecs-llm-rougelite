# Avoid Browser Persistence

The initial game does not persist player preferences, runs, or leaderboard data in the browser. Language defaults to Chinese and may be changed only on the home screen for the current session, keeping persistence centralized on the server and avoiding local browser state that can drift from the intended run flow.

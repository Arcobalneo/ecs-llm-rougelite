# Share Wishcraft Configuration Across Client and Server

The monorepo keeps wishcraft configuration as a shared source of truth for both the browser client and the server. The server uses the same mechanic pieces, visual pieces, parameters, budgets, themes, and rival mappings to constrain wish interpretation that the client uses to simulate and present the awarded wishcraft, preventing drift between validation and gameplay.
